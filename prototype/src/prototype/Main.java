package prototype;

public class Main {
	// this type usually more usable in dynamic type languages (js)
	// where attrribute could dynamically added 
	
	public Main() {
		Car a = new Car(4);
		Car b = a.clone();
	}

}
